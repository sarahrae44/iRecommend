<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="../css/main.css">
    <title></title>
  </head>
  <body>
    <header>
      <h1>Users</h1>
      <nav>
        <form>
          <input type="button" value="Home" onclick="window.location.href='/'"/>
          <input type="button" value="Create an account" onclick="window.location.href='/sessions/register'"/>
          <input type="button" value="Recommendations" onclick="window.location.href='/recs'"/>
        </form>
      </nav>
    </header>
    <main>
      <ul>
        <% for(let i = 0; i < users.length; i++) { %>
        <li>
          <a href="/users/<%=users[i]._id%>"><%=users[i].username%></a>
          <%=users[i].recs%>
        </li>
        <% } %>
      </ul>
    </main>
  </body>
</html>
